<%= link_to gravatar_for(current_user, size: Settings.gravatar_user_size),
  current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("micropost.view_my_profile"), current_user %></span>
<span><%= t("micropost.micropost", count: current_user.microposts.count) %></span>
