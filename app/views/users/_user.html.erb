<li>
  <%= gravatar_for user, size: Settings.gravatar_user_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("users.destroy.delete"), user, method: :delete,
      data: { confirm: t("users.destroy.you_sure") } %>
  <% end %>
</li>
